[project]
name = "developer-tools-backend"
version = "1.0.0"
description = "FastAPI backend for the developer-tools web app"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.32",
    "pydantic>=2.9",
    "pydantic-settings>=2.6",
    "python-multipart>=0.0.31",
    "slowapi>=0.1.9",
    "redis>=5.2",
    "python-dotenv>=1.0",
    "python-dateutil>=2.9",
    "pytz>=2024.2",
    "jsonschema>=4.23",
    "genson>=1.3",
    "exrex>=0.11",
    "faker>=33",
    "pyjwt>=2.13.0",
    "markdown>=3.7",
    "reportlab>=4.2",
    "pypdf>=6.12.0",
    "pillow>=11",
    "pyyaml>=6.0",
    "tomli>=2.0; python_version<'3.11'",
    "tomli-w>=1.0",
    "xmltodict>=0.14",
    "jsonpath-ng>=1.6",
    "croniter>=3.0",
    "qrcode>=8.0",
]

[dependency-groups]
dev = [
    "pytest>=8",
    "pytest-cov>=6",
    "httpx>=0.27",
    "ruff>=0.7",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra -q"

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
extend-select = ["I", "UP"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["app"]
